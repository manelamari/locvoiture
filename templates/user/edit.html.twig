{% extends 'base.html.twig' %}

{% block title %}Modification de profil{% endblock %}

{% block body %}





        <section>
            <div class="container">
                {% if notification %}
                    <div class="alert alert-success">
                        {{ notification }}
                    </div>
                {% endif %}
                <h1>Modification de profil de {{ app.user.nom }} {{ app.user.prenom }}</h1>



                {{ include('user/_form.html.twig', { 'button_label'  : 'Modifier'}) }}


                <a href="{{ path('app_user_index') }}" class="btn btn-lg btn-primary mt-3">back to list</a>

                {{ include('user/_delete_form.html.twig') }}

            </div>
        </section>


{% endblock %}

